import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.mapcalccopymain222',
  appName: 'map_calc-copy-main222',
  webDir: 'dist'
};

export default config;
